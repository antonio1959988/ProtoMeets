<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ProtoMeets - <%- nombrePagina %></title>
    <link rel="stylesheet" href="https://necolas.github.io/normalize.css/8.0.1/normalize.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.8.0/dist/leaflet.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&family=Staatliches&display=swap"
        rel="stylesheet">
        <link rel="stylesheet" href="/css/trix.css">
    <link rel="stylesheet" href="/css/app.css">
</head>

<body>
    <header class="site-header contenedor">
        <a href="/">
            <img src="/img/logo.png" alt="Logo ProtoMeets">
        </a>

        <nav class="nav">
            <% if(locals.usuario.id != null) { %>
                <%- include ('partials/nav-autenticado.ejs') %>
            <% } else { %>
                <%- include ('partials/nav-no-autenticado.ejs') %>
            <% } %>
        </nav>
    </header>

    <% if(locals.mensajes) {%>
        <% const categoria = Object.keys(locals.mensajes) %>
        <% if(categoria.length) { %>
            <% locals.mensajes[categoria].map(alerta => { %>
                <div class="alertas">
                    <p class="alerta <%= categoria %>"><%= alerta%></p>
                </div>
            <% }) %>
        <% } %>
    <% } %>

    <%- body %>

    <footer class="site-footer contenedor">
        <nav class="nav">
            <a href="#">Crear Grupo</a>
            <a href="/iniciar-sesion">Iniciar Sesiòn</a>
            <a href="/crear-cuenta">Registrarse</a>
        </nav>

        <p class="copyright">Todos los derechos reservados ProtoMeets <%- year %> </p>
    </footer>

    <script src="/js/trix.js"></script>

 <script src="https://unpkg.com/leaflet@1.8.0/dist/leaflet.js"></script> 

    <script src="https://unpkg.com/esri-leaflet@3.0.8/dist/esri-leaflet.js"></script>

    <script src="https://unpkg.com/esri-leaflet-geocoder@2.2.13/dist/esri-leaflet-geocoder.js"></script>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet-geosearch/2.7.0/bundle.min.js"></script>

    <script src="/dist/bundle.js"></script>
    <script src="/js/evento.js"></script>
</body>

</html>